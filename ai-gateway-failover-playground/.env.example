# Optional future provider credentials. The MVP uses deterministic provider mocks.
AI_GATEWAY_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# Shared evidence persistence. The app falls back to deterministic mode without these.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never expose this in client code.
SUPABASE_SERVICE_ROLE_KEY=
